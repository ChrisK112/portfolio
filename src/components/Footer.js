import React from "react";

function Footer(){
    return(
        <footer>
          I am a footer that will contain contact info, and credits, and so on. Add credit for icons to icons8.com !
         </footer>

        
    )
    
}

export default Footer;