import React from "react";

function ProjectPageSpace(props){
    return(
        <div className = "projectSpacePage">
            {props.children}
        </div>
    )
    
}

export default ProjectPageSpace;